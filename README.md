# Flight Ticket Booking System

A complete flight ticket booking system with FastAPI backend, React frontend, and Python SDK.

## Setup

1. Run the setup script:
```bash
chmod +x scripts/setup.sh
./scripts/setup.sh